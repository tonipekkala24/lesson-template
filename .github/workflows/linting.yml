name: Linting
on: workflow_call
permissions: 
  contents: write
jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: dependencies
        run: npm ci
      - name: oxlint
        run: npm run lint


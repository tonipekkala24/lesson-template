name: Module tests
on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@7
      - name: dependencies
        run: npm ci
      - name: moduletests
        run: npm 
